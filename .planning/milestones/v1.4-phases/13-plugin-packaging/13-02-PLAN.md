---
phase: 13-plugin-packaging
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [hooks/hooks.json]
autonomous: true
requirements: [DIST-01, DIST-04]
must_haves:
  truths:
    - "hooks/hooks.json exists with valid JSON and 4 event keys (Stop, Notification, StopFailure, SubagentStop)"
    - "Each event has exactly 2 hook entries: one with shell=bash, one with shell=powershell"
    - "All script paths use ${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.sh or .ps1 prefix"
    - "All audio paths use ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-{type}.mp3 pattern"
    - "No hardcoded absolute paths exist in hooks.json"
    - "Audio files referenced in hooks.json exist at audio/voices/gentle/notify-{type}.mp3"
  artifacts:
    - path: "hooks/hooks.json"
      provides: "Hook definitions for 4 Claude Code events with dual-platform support"
      exports: ["Stop", "Notification", "StopFailure", "SubagentStop"]
  key_links:
    - from: "hooks/hooks.json"
      to: "scripts/notify-play.sh"
      via: "${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.sh"
      pattern: '\$\{CLAUDE_PLUGIN_ROOT\}/scripts/notify-play\.sh'
    - from: "hooks/hooks.json"
      to: "scripts/notify-play.ps1"
      via: "${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.ps1"
      pattern: '\$\{CLAUDE_PLUGIN_ROOT\}/scripts/notify-play\.ps1'
    - from: "hooks/hooks.json"
      to: "audio/voices/"
      via: "${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/"
      pattern: '\$\{user_config\.voice\}'
---

<objective>
Create hooks/hooks.json with all 4 Claude Code hook events, dual-platform support (bash + powershell), and ${CLAUDE_PLUGIN_ROOT} portable path resolution. Validate the complete plugin structure.

Purpose: Enables Claude Code to register hooks for task completion notifications (DIST-01) with fully portable paths via ${CLAUDE_PLUGIN_ROOT} (DIST-04). No hardcoded absolute paths.

Output: hooks/hooks.json with Stop, Notification, StopFailure, SubagentStop events, each with bash + powershell hook entries using ${CLAUDE_PLUGIN_ROOT} and ${user_config.voice} substitution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-plugin-packaging/13-CONTEXT.md
@.planning/phases/13-plugin-packaging/13-RESEARCH.md
@.planning/phases/13-plugin-packaging/13-01-PLAN.md
@scripts/notify-play.sh
@scripts/notify-play.ps1
@.claude-plugin/plugin.json
</context>

<interfaces>
<!-- Existing interfaces the executor must use -->

From scripts/notify-play.sh (line 1-2):
```
# Usage: notify-play.sh <type> <audio_file>
# Accepts: type (complete|confirm|error|progress), audio_file (path to mp3)
```

From scripts/notify-play.ps1 (line 1-2):
```
# Usage: powershell -File notify-play.ps1 <type> <audio_file>
# Accepts: $Type (complete|confirm|error|progress), $AudioFile (path to mp3)
```

Event-to-notification mapping (from install.sh lines 89-92):
- Stop -> type=complete, notify-complete.mp3
- Notification -> type=confirm, notify-confirm.mp3
- StopFailure -> type=error, notify-error.mp3
- SubagentStop -> type=progress, notify-progress.mp3

From .claude-plugin/plugin.json:
- userConfig.voice.default = "gentle"
- Variable expansion: ${user_config.voice} resolves to user's selected voice

Available audio directories:
- audio/voices/gentle/ (4 mp3 files)
- audio/voices/deep/ (4 mp3 files)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create hooks/hooks.json with 4 dual-platform hook events</name>
  <files>hooks/hooks.json</files>
  <read_first>
    - .planning/phases/13-plugin-packaging/13-RESEARCH.md (Pattern 2: hooks.json with ${CLAUDE_PLUGIN_ROOT} and ${user_config.KEY} — has exact JSON structure and code example)
    - .planning/phases/13-plugin-packaging/13-CONTEXT.md (D-03: TWO entries per event bash+powershell, D-04: shell field selection, D-06: ${CLAUDE_PLUGIN_ROOT} path pattern, D-07: validation approach)
    - scripts/notify-play.sh (confirm usage: notify-play.sh &lt;type&gt; &lt;audio_file&gt;)
    - scripts/notify-play.ps1 (confirm usage: powershell -File notify-play.ps1 &lt;type&gt; &lt;audio_file&gt;)
    - .claude-plugin/plugin.json (confirm userConfig.voice schema for variable expansion)
  </read_first>
  <action>
    Create directory `hooks/` at repo root and file `hooks/hooks.json` with the following exact content:

    ```json
    {
      "Stop": [
        {
          "hooks": [
            {
              "type": "command",
              "shell": "bash",
              "command": "${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.sh complete ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-complete.mp3",
              "async": true,
              "timeout": 10
            },
            {
              "type": "command",
              "shell": "powershell",
              "command": "powershell -File ${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.ps1 complete ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-complete.mp3",
              "async": true,
              "timeout": 10
            }
          ]
        }
      ],
      "Notification": [
        {
          "hooks": [
            {
              "type": "command",
              "shell": "bash",
              "command": "${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.sh confirm ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-confirm.mp3",
              "async": true,
              "timeout": 10
            },
            {
              "type": "command",
              "shell": "powershell",
              "command": "powershell -File ${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.ps1 confirm ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-confirm.mp3",
              "async": true,
              "timeout": 10
            }
          ]
        }
      ],
      "StopFailure": [
        {
          "hooks": [
            {
              "type": "command",
              "shell": "bash",
              "command": "${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.sh error ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-error.mp3",
              "async": true,
              "timeout": 10
            },
            {
              "type": "command",
              "shell": "powershell",
              "command": "powershell -File ${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.ps1 error ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-error.mp3",
              "async": true,
              "timeout": 10
            }
          ]
        }
      ],
      "SubagentStop": [
        {
          "hooks": [
            {
              "type": "command",
              "shell": "bash",
              "command": "${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.sh progress ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-progress.mp3",
              "async": true,
              "timeout": 10
            },
            {
              "type": "command",
              "shell": "powershell",
              "command": "powershell -File ${CLAUDE_PLUGIN_ROOT}/scripts/notify-play.ps1 progress ${CLAUDE_PLUGIN_ROOT}/audio/voices/${user_config.voice}/notify-progress.mp3",
              "async": true,
              "timeout": 10
            }
          ]
        }
      ]
    }
    ```

    Per D-03: Each event has exactly TWO entries — one with `"shell": "bash"` and one with `"shell": "powershell"`.
    Per D-04: Claude Code auto-selects the correct shell per platform from the `"shell"` field.
    Per D-06: All paths use `${CLAUDE_PLUGIN_ROOT}` prefix. Audio paths use `${user_config.voice}` for voice selection.
    Per RESEARCH Pattern 2: `"async": true` and `"timeout": 10` on all hooks.
    Per RESEARCH Pitfall 4: PowerShell uses `powershell -File <path> <args>` pattern (simple, no complex quoting).

    Event-to-type-to-file mapping (must match install.sh lines 89-92):
    - Stop -> complete -> notify-complete.mp3
    - Notification -> confirm -> notify-confirm.mp3
    - StopFailure -> error -> notify-error.mp3
    - SubagentStop -> progress -> notify-progress.mp3

    Verify the file is valid JSON by running: `jq . hooks/hooks.json`
  </action>
  <verify>
    <automated>jq . hooks/hooks.json && jq -e '.Stop and .Notification and .StopFailure and .SubagentStop' hooks/hooks.json</automated>
  </verify>
  <done>
    - `hooks/hooks.json` exists with valid JSON
    - Contains 4 top-level keys: Stop, Notification, StopFailure, SubagentStop
    - Each event has 2 hooks (bash + powershell)
    - All 8 commands contain `${CLAUDE_PLUGIN_ROOT}`
    - All 8 commands contain `${user_config.voice}`
    - No hardcoded absolute paths (no /home/, no /tmp/, no ~/)
  </done>
  <acceptance_criteria>
    - File `hooks/hooks.json` exists and `jq . hooks/hooks.json` exits 0
    - `jq 'keys' hooks/hooks.json` outputs exactly `["Notification", "Stop", "StopFailure", "SubagentStop"]`
    - `grep -c '"shell": "bash"' hooks/hooks.json` outputs `4` (one per event)
    - `grep -c '"shell": "powershell"' hooks/hooks.json` outputs `4` (one per event)
    - `grep -c 'CLAUDE_PLUGIN_ROOT' hooks/hooks.json` outputs `16` (2 per hook entry: script path + audio path, 8 entries total)
    - `grep -c 'user_config.voice' hooks/hooks.json` outputs `8` (one audio path per hook entry)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Validate plugin structure and verify audio path references</name>
  <files>hooks/hooks.json, .claude-plugin/plugin.json</files>
  <read_first>
    - .planning/phases/13-plugin-packaging/13-CONTEXT.md (D-07: structure validation only, D-08: test audio files exist)
    - .planning/phases/13-plugin-packaging/13-RESEARCH.md (Inline Validation Script section — has exact jq commands)
    - hooks/hooks.json (file being validated)
    - .claude-plugin/plugin.json (file being validated)
  </read_first>
  <action>
    Run the following validation checks from repo root. Do NOT create a validation script file — run inline checks per D-07 Claude's Discretion.

    Check 1 — plugin.json has required fields:
    ```bash
    jq -e '.name and .version and .userConfig.voice' .claude-plugin/plugin.json
    ```

    Check 2 — hooks.json has all 4 event keys:
    ```bash
    jq -e '.Stop and .Notification and .StopFailure and .SubagentStop' hooks/hooks.json
    ```

    Check 3 — plugin.json has no hooks field (Pitfall 1 prevention):
    ```bash
    jq -e '.hooks == null' .claude-plugin/plugin.json
    ```

    Check 4 — Audio files exist for default voice (D-08):
    ```bash
    for type in complete confirm error progress; do
      test -f "audio/voices/gentle/notify-${type}.mp3" || echo "MISSING: audio/voices/gentle/notify-${type}.mp3"
    done
    ```

    Check 5 — Audio files exist for deep voice (D-08):
    ```bash
    for type in complete confirm error progress; do
      test -f "audio/voices/deep/notify-${type}.mp3" || echo "MISSING: audio/voices/deep/notify-${type}.mp3"
    done
    ```

    Check 6 — Scripts referenced in hooks.json exist:
    ```bash
    test -f "scripts/notify-play.sh" && echo "OK: scripts/notify-play.sh" || echo "MISSING: scripts/notify-play.sh"
    test -f "scripts/notify-play.ps1" && echo "OK: scripts/notify-play.ps1" || echo "MISSING: scripts/notify-play.ps1"
    ```

    Check 7 — No hardcoded absolute paths in hooks.json:
    ```bash
    grep -E '/home/|/tmp/|/Users/|~/' hooks/hooks.json && echo "FAIL: hardcoded absolute paths found" || echo "OK: no hardcoded absolute paths"
    ```

    If any check fails, report the failure clearly. Do NOT fix files — report what failed so the executor can address it.

    Optionally, if `claude` CLI is available, run:
    ```bash
    claude plugin validate . 2>&1 || true
    ```
    This may not be available in all environments — treat as informational, not blocking.
  </action>
  <verify>
    <automated>
      jq -e '.name and .version and .userConfig.voice' .claude-plugin/plugin.json &&
      jq -e '.Stop and .Notification and .StopFailure and .SubagentStop' hooks/hooks.json &&
      jq -e '.hooks == null' .claude-plugin/plugin.json &&
      for type in complete confirm error progress; do test -f "audio/voices/gentle/notify-${type}.mp3"; done &&
      for type in complete confirm error progress; do test -f "audio/voices/deep/notify-${type}.mp3"; done &&
      test -f "scripts/notify-play.sh" && test -f "scripts/notify-play.ps1"
    </automated>
  </verify>
  <done>
    - All 7 validation checks pass with no errors
    - plugin.json: valid JSON, has name/version/userConfig, no hooks field
    - hooks.json: valid JSON, has all 4 event keys
    - All 8 audio files exist (4 gentle + 4 deep)
    - Both scripts exist (notify-play.sh, notify-play.ps1)
    - No hardcoded absolute paths in hooks.json
  </done>
  <acceptance_criteria>
    - `jq -e '.name and .version and .userConfig.voice' .claude-plugin/plugin.json` exits 0
    - `jq -e '.Stop and .Notification and .StopFailure and .SubagentStop' hooks/hooks.json` exits 0
    - `jq -e '.hooks == null' .claude-plugin/plugin.json` exits 0
    - All 8 files exist: `test -f audio/voices/gentle/notify-complete.mp3` through `test -f audio/voices/deep/notify-progress.mp3`
    - `grep -cE '/home/|/tmp/|/Users/|~/' hooks/hooks.json` outputs `0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `jq . hooks/hooks.json` — valid JSON
2. `jq 'keys' hooks/hooks.json` — 4 event keys present
3. `grep -c '"shell": "bash"' hooks/hooks.json` — output is 4
4. `grep -c '"shell": "powershell"' hooks/hooks.json` — output is 4
5. `grep -c 'CLAUDE_PLUGIN_ROOT' hooks/hooks.json` — output is 16 (all paths portable)
6. `grep -c 'user_config.voice' hooks/hooks.json` — output is 8 (voice selection in all audio paths)
7. `grep -cE '/home/|/tmp/|/Users/|~/' hooks/hooks.json` — output is 0 (no hardcoded paths)
8. All 8 audio files exist at `audio/voices/{gentle,deep}/notify-{type}.mp3`
9. `claude plugin validate .` (informational, if available)
</verification>

<success_criteria>
- hooks/hooks.json exists with valid JSON and all 4 events
- Each event has bash + powershell entries (8 total hook definitions)
- All paths use ${CLAUDE_PLUGIN_ROOT} and ${user_config.voice} — zero hardcoded absolute paths (DIST-04)
- Plugin structure is valid: plugin.json + hooks/hooks.json + all referenced files exist (DIST-01)
</success_criteria>

<output>
After completion, create `.planning/phases/13-plugin-packaging/13-02-SUMMARY.md`
</output>
