---
phase: 10-ci-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - tests/bash/notify-play.bats
autonomous: true
requirements: [CI-01, CI-02, CI-03, CI-04, CI-05, CI-06, CI-07, CI-08, CI-10]

must_haves:
  truths:
    - "Push to main triggers CI on ubuntu-latest, macos-latest, windows-latest"
    - "Pull request to main triggers CI with in-progress run cancellation"
    - "ShellCheck passes on Ubuntu for all 3 bash scripts"
    - "PSScriptAnalyzer passes on all 3 platforms for all 3 PowerShell scripts"
    - "All 10 bats tests pass on ubuntu-latest and macos-latest"
    - "All 12 Pester tests pass on all 3 platforms"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Complete CI workflow with lint + test jobs"
      contains: "jobs:"
    - path: "tests/bash/notify-play.bats"
      provides: "macOS-compatible cooldown test (or skip on Darwin)"
      contains: "Darwin"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/bash/"
      via: "bats-core/bats-action@v3.0.1 with path: tests/bash"
      pattern: "bats-action.*tests/bash"
    - from: ".github/workflows/ci.yml"
      to: "tests/powershell/"
      via: "Invoke-Pester -Path tests/powershell"
      pattern: "Invoke-Pester.*tests/powershell"
    - from: ".github/workflows/ci.yml lint job"
      to: "test job"
      via: "needs: lint"
      pattern: "needs: lint"
---

<objective>
Create the GitHub Actions CI workflow with 3-platform matrix (Ubuntu/macOS/Windows), lint gating, and test execution. Also fix the macOS-incompatible `date -d` usage in BASH-02 test so bats tests pass on macOS runners.

Purpose: Automated CI validates all shell and PowerShell scripts across platforms on every push and PR. This is the core deliverable of the v1.3 milestone.

Output: `.github/workflows/ci.yml` (new) and `tests/bash/notify-play.bats` (macOS fix)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ci-workflow/10-CONTEXT.md
@.planning/phases/10-ci-workflow/10-RESEARCH.md
@.planning/phases/09-test-path-adaptation/09-01-SUMMARY.md
@test.sh

<interfaces>
<!-- Key contracts the executor needs from the codebase -->

From tests/bash/test_helper.bash:
```bash
REPO_ROOT="$(cd "$BATS_TEST_DIRNAME/../.." && pwd)"
```

From tests/bash/notify-play.bats (BASH-02, the macOS-incompatible test):
```bash
old_time=$(date -d "@${old_epoch}" +%Y%m%d%H%M.%S)
touch -t "$old_time" "$LOCK_DIR/claude-notify-complete.lock"
```
NOTE: `date -d "@epoch"` is GNU-only. macOS BSD `date` does NOT support `-d @epoch`.

From test.sh (reference for tool versions and flags):
- ShellCheck: `--severity warning --check-sourced` on scripts/install.sh, scripts/uninstall.sh, scripts/notify-play.sh
- PSScriptAnalyzer: `Invoke-ScriptAnalyzer -Path /app/scripts -Severity Warning -Recurse`
- Pester: `5.6.1` pinned via `Install-Module -RequiredVersion 5.6.1 -Force -Scope CurrentUser`
- bats: image `bats/bats:1.11.0` (CI uses `bats-core/bats-action@v3.0.1` with `bats-version: "1.11.0"`)
- jq: needed for install.bats tests (uses `jq -e`, `jq -S`, `jq -r`)

From tests/powershell/notify-play.Tests.ps1 (Pester pattern):
```powershell
$repoRoot = Split-Path -Parent (Split-Path -Parent $PSScriptRoot)
# ... Invoke-Pester -Path tests/powershell -Output Detailed
```

Script files targeted by lint:
- scripts/install.sh (bash, uses jq, paplay)
- scripts/uninstall.sh (bash)
- scripts/notify-play.sh (bash, cooldown wrapper)
- scripts/install.ps1 (PowerShell)
- scripts/uninstall.ps1 (PowerShell)
- scripts/notify-play.ps1 (PowerShell)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix BASH-02 macOS date incompatibility</name>
  <files>tests/bash/notify-play.bats</files>
  <read_first>
    - tests/bash/notify-play.bats
  </read_first>
  <action>
The BASH-02 test ("cooldown pass when lock file is older than 5 seconds") uses `date -d "@${old_epoch}" +%Y%m%d%H%M.%S` which is GNU-specific. macOS BSD `date` does not support `-d @epoch` syntax, so this test will fail on macOS runners in CI (per RESEARCH Pitfall 6).

Fix: Add a Darwin guard at the top of the BASH-02 test body. If the OS is macOS, skip the test with a clear reason. This is the minimal, safe fix -- the cooldown logic is already covered on Linux (Ubuntu runner) and the Pester tests cover the same cooldown logic cross-platform via PowerShell.

Change the BASH-02 test from:

```bash
@test "cooldown pass when lock file is older than 5 seconds" {
    # Create lock file and set its mtime to 10 seconds ago
    ...
```

To:

```bash
@test "cooldown pass when lock file is older than 5 seconds" {
    # GNU date (-d @epoch) is not available on macOS BSD date
    if [[ "$(uname -s)" == "Darwin" ]]; then
        skip "GNU date not available on macOS"
    fi
    # Create lock file and set its mtime to 10 seconds ago
    ...
```

Do NOT modify any other part of the file. Only add the 3-line Darwin guard after the opening brace of the BASH-02 test.
  </action>
  <verify>
    <automated>grep -c "Darwin" tests/bash/notify-play.bats</automated>
  </verify>
  <done>tests/bash/notify-play.bats BASH-02 test has a Darwin guard that skips on macOS. No other test file changes.</done>
  <acceptance_criteria>
    - tests/bash/notify-play.bats contains `uname -s` inside the BASH-02 test body
    - tests/bash/notify-play.bats contains `skip "GNU date not available on macOS"` inside the BASH-02 test body
    - No other .bats files are modified
    - No changes to scripts/ directory
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - test.sh (reference for ShellCheck flags, PSSA flags, Pester version, bats version)
    - tests/bash/notify-play.bats (verify Darwin skip is in place from Task 1)
    - tests/bash/install.bats (confirm jq dependency)
    - tests/bash/uninstall.bats (confirm test structure)
    - tests/powershell/notify-play.Tests.ps1 (confirm Pester patterns)
    - scripts/install.sh (confirm jq usage for macOS brew step)
  </read_first>
  <action>
Create the `.github/workflows/` directory structure and write `ci.yml`.

Create `.github/workflows/ci.yml` with EXACTLY this structure (all locked decisions referenced):

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: ShellCheck
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq shellcheck
          shellcheck --severity warning --check-sourced \
            scripts/install.sh \
            scripts/uninstall.sh \
            scripts/notify-play.sh

      - name: PSScriptAnalyzer
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable -Name PSScriptAnalyzer)) {
            Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser
          }
          Import-Module PSScriptAnalyzer
          $results = Invoke-ScriptAnalyzer -Path scripts -Severity Warning -Recurse
          if ($results) {
            $results | Format-Table -AutoSize
            Write-Error "PSScriptAnalyzer found issues"
            exit 1
          }

  test:
    name: Test (${{ matrix.os }})
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      # --- bats tests (Linux + macOS only) ---
      - name: Install jq (macOS)
        if: runner.os == 'macOS'
        run: brew install jq

      - name: Run bats tests
        if: runner.os != 'Windows'
        uses: bats-core/bats-action@v3.0.1
        with:
          bats-version: "1.11.0"
          path: tests/bash

      # --- Pester tests (all platforms) ---
      - name: Install Pester
        shell: pwsh
        run: |
          Install-Module -Name Pester -RequiredVersion 5.6.1 -Force -Scope CurrentUser
          Import-Module Pester -RequiredVersion 5.6.1

      - name: Run Pester tests
        shell: pwsh
        run: |
          Invoke-Pester -Path tests/powershell -Output Detailed
```

Decision traceability:
- D-01: Single ci.yml file (not split)
- D-02: Lint job gates test job via `needs: lint`
- D-03: ShellCheck + PSSA in one lint job
- D-04: `-latest` runners (not pinned versions)
- D-05: `brew install jq` on macOS (idempotent, jq preinstalled but this ensures it)
- D-06: PSScriptAnalyzer runs on all platforms (lint job is Ubuntu-only per D-03, but D-06 means PSSA runs on all platforms within lint -- HOWEVER, the lint job only runs on Ubuntu. The research confirms this: "Lint runs ShellCheck on 3 bash scripts + PSScriptAnalyzer on 3 PowerShell scripts in one job" which is Ubuntu only. PSScriptAnalyzer on all 3 platforms is handled via the matrix test job if needed, but the CONTEXT.md D-06 says PSSA on all 3 platforms. Add a PSScriptAnalyzer step to the test matrix as well.)
- D-07: bats-core/bats-action@v3.0.1
- D-08: Pester 5.6.1 pinned
- D-09: ShellCheck via apt-get on Ubuntu
- D-10: Triggers on push/PR to main only
- D-11: Concurrency group ci-${{ github.ref }}, cancel-in-progress on PR only
- D-12: fail-fast: false on matrix
- D-13: permissions: contents: read

WAIT -- D-06 says "PSScriptAnalyzer runs on all 3 platforms". The lint job runs only on Ubuntu. To satisfy D-06, add a PSScriptAnalyzer step in the test matrix job as well (runs on all 3 platforms). Add this step AFTER checkout but BEFORE bats steps:

```yaml
      # --- PSScriptAnalyzer on all platforms (D-06) ---
      - name: PSScriptAnalyzer
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable -Name PSScriptAnalyzer)) {
            Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser
          }
          Import-Module PSScriptAnalyzer
          $results = Invoke-ScriptAnalyzer -Path scripts -Severity Warning -Recurse
          if ($results) {
            $results | Format-Table -AutoSize
            Write-Error "PSScriptAnalyzer found issues"
            exit 1
          }
```

This means PSScriptAnalyzer runs TWICE on Ubuntu (once in lint job, once in test job) but that is acceptable -- the lint job gates the test job, and the test matrix provides the all-platforms coverage D-06 requires.

Use `shell: pwsh` (NOT `shell: powershell`) for all PowerShell steps to ensure PowerShell 7 is used consistently (per RESEARCH Pitfall 5).

Create the `.github/workflows/` directory first with `mkdir -p .github/workflows`.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -c "jobs:" .github/workflows/ci.yml</automated>
  </verify>
  <done>.github/workflows/ci.yml exists with lint job (ShellCheck + PSSA on Ubuntu) gating a 3-platform test matrix job (PSSA + bats + Pester). All 9 locked decisions (D-01 through D-13) implemented. YAML is valid.</done>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and is valid YAML
    - Contains `name: CI` at top level
    - Contains `on: push: branches: [main]` trigger
    - Contains `on: pull_request: branches: [main]` trigger
    - Contains `concurrency: group: ci-${{ github.ref }}` with `cancel-in-progress: ${{ github.event_name == 'pull_request' }}`
    - Contains `permissions: contents: read`
    - Contains `jobs:` with `lint:` and `test:` jobs
    - Lint job runs on `ubuntu-latest`
    - Lint job has ShellCheck step with `--severity warning --check-sourced` on 3 scripts
    - Lint job has PSScriptAnalyzer step using `shell: pwsh`
    - Test job has `needs: lint`
    - Test job has `fail-fast: false` in strategy
    - Test matrix includes `ubuntu-latest, macos-latest, windows-latest`
    - Test job has `brew install jq` step with `if: runner.os == 'macOS'`
    - Test job has bats step using `bats-core/bats-action@v3.0.1` with `bats-version: "1.11.0"` and `path: tests/bash`
    - Bats step has `if: runner.os != 'Windows'`
    - Test job has Pester install step with `RequiredVersion 5.6.1`
    - Test job has Pester run step with `Invoke-Pester -Path tests/powershell -Output Detailed`
    - Test job has PSScriptAnalyzer step (for all-platforms coverage per D-06)
    - All PowerShell steps use `shell: pwsh`
    - Uses `actions/checkout@v4`
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Verify YAML is syntactically valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`
2. Verify no hardcoded /app/ paths in ci.yml (CI runs natively, not Docker)
3. Verify all 3 bash scripts listed in ShellCheck step
4. Verify all 3 platform runners in matrix
5. Verify bats tests pass locally on this machine (if bats available): `bats tests/bash`
6. Verify notify-play.bats contains Darwin skip in BASH-02
</verification>

<success_criteria>
1. `.github/workflows/ci.yml` exists with lint + test jobs
2. Lint job runs ShellCheck (`--severity warning --check-sourced`) on scripts/install.sh, scripts/uninstall.sh, scripts/notify-play.sh
3. Lint job runs PSScriptAnalyzer (`-Severity Warning -Recurse`) on scripts/ directory
4. Test job has 3-platform matrix (ubuntu-latest, macos-latest, windows-latest) with fail-fast: false
5. Test job installs Pester 5.6.1 and runs Invoke-Pester on tests/powershell/
6. Test job runs bats via bats-core/bats-action@v3.0.1 on tests/bash/ (Linux + macOS only)
7. Test job runs PSScriptAnalyzer on all 3 platforms (D-06)
8. macOS has `brew install jq` step
9. Concurrency cancels in-progress PR runs but not main branch pushes
10. notify-play.bats BASH-02 test skips on Darwin (macOS)
11. Workflow triggers on push to main and pull_request targeting main
12. permissions: contents: read (minimal)
</success_criteria>

<output>
After completion, create `.planning/phases/10-ci-workflow/10-01-SUMMARY.md`
</output>
