---
phase: 11-readme-ci-badge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: false
requirements: [CI-11]

must_haves:
  truths:
    - "CI status badge renders at top of README pointing to ci.yml workflow"
    - "Installation commands are copy-paste ready for Linux/macOS and Windows"
    - "Hook configuration example shows the 4 Claude Code hook events"
    - "README is scannable in under 30 seconds"
  artifacts:
    - path: "README.md"
      provides: "Project README with CI badge, install instructions, hook config"
      contains: "actions/workflows/ci.yml/badge.svg"
  key_links:
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "CI badge URL"
      pattern: "actions/workflows/ci\\.yml/badge\\.svg"
    - from: "README.md"
      to: "scripts/install.sh"
      via: "Linux/macOS install command"
      pattern: "install\\.sh"
    - from: "README.md"
      to: "scripts/install.ps1"
      via: "Windows install command"
      pattern: "install\\.ps1"
---

<objective>
Create README.md at repo root with CI status badge, tri-platform install instructions, and hook configuration example.

Purpose: Complete CI-11 requirement. README is the landing page for the repo -- users need to understand what this does and how to set it up in under 30 seconds.

Output: README.md at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-readme-ci-badge/11-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create README.md with CI badge, install, and hook config</name>
  <files>README.md</files>
  <read_first>
    - scripts/install.sh (for exact Linux/macOS install command syntax -- it takes no flags, derives REPO_ROOT from SCRIPT_DIR)
    - scripts/install.ps1 (for exact Windows install command syntax -- takes -RepoPath parameter)
    - tests/fixtures/settings.json (for hook configuration pattern reference)
    - .github/workflows/ci.yml (for badge URL verification)
    - .planning/phases/11-readme-ci-badge/11-CONTEXT.md (for locked decisions D-01 through D-06)
  </read_first>
  <action>
Create README.md at repo root. English language (per D-01). Concise format (per D-02). Total under 80 lines.

Section structure (per D-06, in this exact order):

**1. Title + one-liner + CI badge (top)**

Title: `# Claude Code Voice Notifications`

One-liner description: "Cross-platform voice notifications for Claude Code. Get audio alerts when tasks complete, need input, or fail -- so you don't have to keep checking the terminal."

CI badge on the line right after (per D-03, D-04):
```markdown
![CI](https://github.com/hlwqds/notify-research/actions/workflows/ci.yml/badge.svg)
```

**2. Install section (per D-05)**

Use section headers with platform labels to separate. Two platform code blocks:

Linux / macOS -- install.sh takes NO flags, it derives paths from its own location:
```bash
git clone https://github.com/hlwqds/notify-research.git
cd notify-research
bash scripts/install.sh
```

Windows -- install.ps1 requires -RepoPath parameter:
```powershell
git clone https://github.com/hlwqds/notify-research.git
cd notify-research
powershell -File scripts/install.ps1 -RepoPath (Get-Location)
```

Prerequisites line: "Requires: Claude Code >= 2.1.78, jq + paplay (Linux), afplay (macOS). No extra deps on Windows."

**3. Hook configuration example**

Brief explanation of the 4 hook events installed (matching install.sh event mapping):
- `Stop` -> notify-complete.mp3 (task finished)
- `Notification` -> notify-confirm.mp3 (needs user input)
- `StopFailure` -> notify-error.mp3 (task failed)
- `SubagentStop` -> notify-progress.mp3 (sub-agent finished)

Show a minimal settings.json snippet for one event as illustration:
```json
{
  "hooks": {
    "Stop": [{
      "hooks": [{
        "type": "command",
        "command": "/path/to/scripts/notify-play.sh complete ~/.claude/notify-complete.mp3",
        "async": true,
        "timeout": 10
      }]
    }]
  }
}
```

Add note: "The install script configures all 4 events automatically. This example shows the structure for reference."

**4. Uninstall**

Brief section:
```bash
# Linux/macOS
bash scripts/uninstall.sh

# Windows
powershell -File scripts/uninstall.ps1
```

**5. Links**

- [Spark-TTS](https://github.com/SparkAudio/Spark-TTS) -- TTS engine
- License: Apache 2.0

**Do NOT include:**
- Docker/generate.sh instructions (TTS regeneration is a dev concern)
- Architecture diagrams or developer guides
- License badge or platform badges (per D-03)
  </action>
  <acceptance_criteria>
    - `README.md` exists at repo root
    - `grep -c '.' README.md` returns less than 80 (under 80 lines)
    - `grep -q 'actions/workflows/ci.yml/badge.svg' README.md` passes (CI badge present)
    - `grep -q 'bash scripts/install.sh' README.md` passes (Linux/macOS install command)
    - `grep -q 'scripts/install.ps1' README.md` passes (Windows install command)
    - `grep -q 'Stop' README.md` passes (Stop hook event)
    - `grep -q 'Notification' README.md` passes (Notification hook event)
    - `grep -q 'StopFailure' README.md` passes (StopFailure hook event)
    - `grep -q 'SubagentStop' README.md` passes (SubagentStop hook event)
    - `grep -q 'Spark-TTS' README.md` passes (Spark-TTS link)
    - `grep -q 'Apache 2.0' README.md` passes (license mention)
    - No Chinese characters in README.md (D-01: English only)
  </acceptance_criteria>
  <verify>
    <automated>test -f README.md && grep -q 'actions/workflows/ci.yml/badge.svg' README.md && grep -q 'install.sh' README.md && grep -q 'install.ps1' README.md && grep -q 'Stop' README.md && grep -q 'Notification' README.md && grep -q 'StopFailure' README.md && grep -q 'SubagentStop' README.md && echo "README.md validation passed"</automated>
  </verify>
  <done>
    - README.md exists at repo root, under 80 lines
    - Contains CI badge URL: `https://github.com/hlwqds/notify-research/actions/workflows/ci.yml/badge.svg`
    - Contains copy-paste install commands: `bash scripts/install.sh` for Linux/macOS, `powershell -File scripts/install.ps1 -RepoPath (Get-Location)` for Windows
    - Lists all 4 hook events (Stop, Notification, StopFailure, SubagentStop)
    - Links to Spark-TTS source, mentions Apache 2.0 license
    - English language throughout, no Chinese text
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify README renders correctly</name>
  <files>README.md</files>
  <action>Present README.md to user for visual verification of CI badge rendering, install command accuracy, hook config clarity, and overall readability.</action>
  <verify>User types "approved" confirming README renders correctly and all sections are clear.</verify>
  <done>User has approved README.md content, CI badge, install commands, and hook configuration section.</done>
  <what-built>README.md at repo root with CI badge, tri-platform install instructions, and hook configuration example.</what-built>
  <how-to-verify>
    1. Open README.md in a text editor or markdown preview and verify:
       - CI badge markdown is correct: `![CI](https://github.com/hlwqds/notify-research/actions/workflows/ci.yml/badge.svg)`
       - Install commands are copy-paste ready for both platforms
       - Hook configuration section shows all 4 events
       - README is scannable in under 30 seconds
       - No Chinese text (D-01: English only)
    2. Verify badge URL resolves: `curl -sI https://github.com/hlwqds/notify-research/actions/workflows/ci.yml/badge.svg` should return HTTP 200
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. README.md exists at repo root
2. CI badge URL is correct: `![CI](https://github.com/hlwqds/notify-research/actions/workflows/ci.yml/badge.svg)`
3. Install commands reference scripts/install.sh and scripts/install.ps1 accurately
4. All 4 hook events mentioned (Stop, Notification, StopFailure, SubagentStop)
5. English language throughout, concise format, under 80 lines
</verification>

<success_criteria>
- README.md at repo root with project description, CI badge, install, hook config, and links
- Badge URL points to the actual ci.yml workflow file
- Install commands match the actual script interfaces (no made-up flags)
- README is under 80 lines and scannable in 30 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/11-readme-ci-badge/11-01-SUMMARY.md`
</output>
