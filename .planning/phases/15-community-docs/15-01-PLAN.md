---
phase: 15-community-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [LICENSE, README.md]
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "LICENSE file exists with MIT License content"
    - "README.md documents plugin-based install as primary"
    - "README.md documents curl|bash/irm|iex one-liners as fallback"
  artifacts:
    - path: "LICENSE"
      provides: "Legal permission for community use"
    - path: "README.md"
      provides: "Project documentation and installation guide"
---

<objective>
Adopt the MIT License and overhaul the README.md to prioritize the Claude Code plugin installation method.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/15-community-docs/15-RESEARCH.md
@.planning/phases/15-community-docs/15-CONTEXT.md
@.claude-plugin/plugin.json
@hooks/hooks.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MIT LICENSE file</name>
  <files>LICENSE</files>
  <action>
    Create a LICENSE file in the project root with the standard MIT License text.
    Year: 2026
    Copyright holder: hlwqds
  </action>
  <verify>
    <automated>grep -q "MIT License" LICENSE && grep -q "2026 hlwqds" LICENSE</automated>
  </verify>
  <done>LICENSE file exists and contains the correct MIT License text.</done>
</task>

<task type="auto">
  <name>Task 2: Overhaul README.md</name>
  <files>README.md</files>
  <action>
    Overhaul the README.md to reflect the project's state as a Claude Code notification plugin.
    
    Structure:
    1. Header: Project name "Claude Code Voice Notifications", brief description, and CI badge.
    2. Plugin Installation (Primary):
       - Step 1: `/plugin marketplace add hlwqds/notify-research`
       - Step 2: `/plugin install claude-voice-notify@hlwqds`
    3. One-Liner Installation (Alternative):
       - Linux/macOS: `curl -fsSL https://raw.githubusercontent.com/hlwqds/notify-research/main/scripts/install-online.sh | bash`
       - Windows (PowerShell): `irm https://raw.githubusercontent.com/hlwqds/notify-research/main/scripts/install-online.ps1 | iex`
    4. Voice Configuration:
       - Explain how to switch voices using `/claude-voice-notify:configure` (Plugin) or `bash scripts/install.sh --voice <name>` (Legacy).
       - List available voices (gentle, deep).
    5. How it Works (Hooks):
       - Table of the 4 events (Stop, Notification, StopFailure, SubagentStop) and their triggers.
    6. Requirements:
       - Claude Code >= 2.1.88
       - OS: macOS (afplay), Linux (paplay + jq), Windows (Native).
    7. Uninstallation:
       - Plugin: `/plugin uninstall claude-voice-notify`
       - Legacy: `bash scripts/uninstall.sh` or `powershell -File scripts/uninstall.ps1`
    8. License:
       - Explicitly state "MIT License" and link to the LICENSE file.
  </action>
  <verify>
    <automated>grep -q "/plugin marketplace add" README.md && grep -q "install-online.sh" README.md && grep -q "MIT License" README.md</automated>
  </verify>
  <done>README.md updated with correct installation, configuration, and license info.</done>
</task>

</tasks>

<verification>
Check that LICENSE exists and README.md correctly documents the plugin installation flow.
</verification>

<success_criteria>
- Project has a valid MIT LICENSE.
- README.md provides a modern, plugin-first installation guide.
</success_criteria>

<output>
After completion, create `.planning/phases/15-community-docs/15-01-SUMMARY.md`
</output>
