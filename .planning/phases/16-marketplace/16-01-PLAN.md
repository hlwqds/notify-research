---
phase: 16-marketplace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - .claude-plugin/plugin.json
autonomous: true
requirements:
  - MKT-01
  - MKT-02
  - MKT-03
  - MKT-04
  - VAL-01

must_haves:
  truths:
    - "User can add the marketplace via /plugin marketplace add hlwqds/notify-research"
    - "marketplace.json has name=hlwqds, owner info, and a single plugin entry with source ./"
    - "plugin.json has author, license, homepage, repository, and keywords fields"
    - "hooks.json uses ${CLAUDE_PLUGIN_ROOT} for all 8 hook commands (no regression)"
    - "claude plugin validate . exits with zero errors"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace catalog with name, owner, and plugin entry"
      contains: "\"name\": \"hlwqds\""
    - path: ".claude-plugin/plugin.json"
      provides: "Enriched plugin manifest with marketplace metadata"
      contains: "\"license\": \"MIT\""
    - path: "hooks/hooks.json"
      provides: "Hooks configuration (verify-only, no changes expected)"
      contains: "CLAUDE_PLUGIN_ROOT"
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: ".claude-plugin/plugin.json"
      via: "Plugin entry source=./ points to repo root where plugin.json lives"
      pattern: "\"source\": \"./\""
    - from: "hooks/hooks.json"
      to: "${CLAUDE_PLUGIN_ROOT}"
      via: "All 8 hook commands reference scripts via plugin root variable"
      pattern: "\\$\\{CLAUDE_PLUGIN_ROOT\\}"
---

<objective>
Create marketplace.json and enrich plugin.json for Claude Code plugin marketplace distribution.

Purpose: Enable users to discover and install claude-voice-notify via `/plugin marketplace add hlwqds/notify-research` and `/plugin install claude-voice-notify@hlwqds`. This is the core deliverable of Phase 16 -- the marketplace catalog file and enriched plugin manifest.

Output: `.claude-plugin/marketplace.json` (new), `.claude-plugin/plugin.json` (enriched), validated via `claude plugin validate .`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-marketplace/16-CONTEXT.md
@.planning/phases/16-marketplace/16-RESEARCH.md

<interfaces>
<!-- Current plugin.json that will be enriched (NOT replaced) -->
From .claude-plugin/plugin.json:
```json
{
  "name": "claude-voice-notify",
  "version": "1.4.0",
  "description": "Chinese voice notifications for Claude Code task events. Plays audio on task complete, confirmation, error, and progress.",
  "userConfig": {
    "voice": {
      "type": "string",
      "default": "gentle",
      "description": "Voice pack name (available: gentle, deep)",
      "title": "Voice"
    }
  }
}
```

<!-- hooks.json already portable, verify-only in this phase -->
From hooks/hooks.json:
- 4 events: Stop, Notification, StopFailure, SubagentStop
- 2 platforms each: bash + powershell = 8 total hooks
- All 8 commands use `${CLAUDE_PLUGIN_ROOT}` prefix
- No changes needed to this file (MKT-04 is verify-only)

<!-- Key constants from CONTEXT.md locked decisions -->
- Marketplace name: "hlwqds" (D-01)
- Owner name: user's display name (D-02)
- Plugin source: "./" relative path (D-03)
- Keywords: ["notification", "audio", "voice", "chinese", "tts"] (D-04)
- Category: "productivity" (D-04)
- Plugin.json enriched fields: author, license "MIT", homepage, repository, keywords (D-05)
- GitHub repo: hlwqds/notify-research
- Version stays at 1.4.0 (bump to 1.5.0 is Phase 17 DOC-02)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace.json and enrich plugin.json</name>
  <files>.claude-plugin/marketplace.json, .claude-plugin/plugin.json</files>
  <read_first>
    - .claude-plugin/plugin.json (current manifest to enrich, not replace)
    - hooks/hooks.json (verify existing ${CLAUDE_PLUGIN_ROOT} usage)
    - README.md (confirm marketplace name "hlwqds" matches install instructions)
  </read_first>
  <action>
Create `.claude-plugin/marketplace.json` with the following exact content (per D-01 through D-04):

```json
{
  "name": "hlwqds",
  "owner": {
    "name": "hlwqds",
    "email": "hlwqds@users.noreply.github.com"
  },
  "metadata": {
    "description": "Cross-platform voice notifications for Claude Code"
  },
  "plugins": [
    {
      "name": "claude-voice-notify",
      "source": "./",
      "description": "Chinese voice notifications for Claude Code task events. Plays audio on task complete, confirmation, error, and progress.",
      "version": "1.4.0",
      "author": {
        "name": "hlwqds"
      },
      "keywords": ["notification", "audio", "voice", "chinese", "tts"],
      "category": "productivity"
    }
  ]
}
```

Key details (from RESEARCH.md pitfalls):
- `source` is `"./"` (relative to repo root, NOT `"../"` or `"./plugin"`) per D-03
- `author` is an object with `name` field, NOT a bare string (Pitfall 2)
- No `license`, `homepage`, `repository` in marketplace entry -- these belong in plugin.json per D-05
- No `strict` field -- use default `true` (plugin.json is authority, marketplace supplements)
- No `tags` array -- same content as keywords, no benefit to duplication
- `metadata.description` included (Claude's discretion -- useful for discovery)
- `owner.email` uses GitHub noreply format (RESEARCH.md Open Question 1 resolution)

Then enrich `.claude-plugin/plugin.json` by adding these fields to the EXISTING content (do NOT remove or change existing fields: name, version, description, userConfig):

Add these fields after `description`:
```json
"author": {
  "name": "hlwqds"
},
"license": "MIT",
"homepage": "https://github.com/hlwqds/notify-research",
"repository": "https://github.com/hlwqds/notify-research",
"keywords": ["notification", "audio", "voice", "chinese", "tts"],
```

The final plugin.json must contain ALL of these fields in order: name, version, description, author, license, homepage, repository, keywords, userConfig. Keep the existing `userConfig` block unchanged.

IMPORTANT:
- `author` must be an object `{"name": "hlwqds"}`, NOT a bare string (Pitfall 2 from RESEARCH.md)
- Version stays at `"1.4.0"` -- the bump to 1.5.0 is Phase 17 (DOC-02)
- Do NOT add a `hooks` field to plugin.json (Phase 13 Pitfall 1 avoidance)
- Do NOT modify hooks/hooks.json -- it already uses ${CLAUDE_PLUGIN_ROOT} everywhere
  </action>
  <verify>
    <automated>cd /home/huanglin/code/claude-config/notify-research && python3 -c "
import json, sys

# Validate marketplace.json
with open('.claude-plugin/marketplace.json') as f:
    m = json.load(f)
assert m['name'] == 'hlwqds', 'marketplace name must be hlwqds'
assert m['owner']['name'] == 'hlwqds', 'owner name must be hlwqds'
assert 'email' in m['owner'], 'owner must have email'
assert len(m['plugins']) == 1, 'must have exactly 1 plugin entry'
p = m['plugins'][0]
assert p['name'] == 'claude-voice-notify', 'plugin name must be claude-voice-notify'
assert p['source'] == './', 'source must be ./'
assert p['keywords'] == ['notification', 'audio', 'voice', 'chinese', 'tts'], 'keywords mismatch'
assert p['category'] == 'productivity', 'category must be productivity'
assert isinstance(p['author'], dict) and 'name' in p['author'], 'author must be object with name'

# Validate plugin.json enriched fields
with open('.claude-plugin/plugin.json') as f:
    pj = json.load(f)
assert pj['name'] == 'claude-voice-notify', 'plugin name unchanged'
assert pj['version'] == '1.4.0', 'version must stay 1.4.0'
assert isinstance(pj.get('author'), dict) and pj['author']['name'] == 'hlwqds', 'author must be object'
assert pj['license'] == 'MIT', 'license must be MIT'
assert pj['homepage'] == 'https://github.com/hlwqds/notify-research', 'homepage URL'
assert pj['repository'] == 'https://github.com/hlwqds/notify-research', 'repository URL'
assert pj['keywords'] == ['notification', 'audio', 'voice', 'chinese', 'tts'], 'keywords mismatch'
assert 'userConfig' in pj, 'userConfig must be preserved'
assert pj['userConfig']['voice']['default'] == 'gentle', 'voice default preserved'

# Validate hooks.json uses CLAUDE_PLUGIN_ROOT (no regression)
with open('hooks/hooks.json') as f:
    h = json.load(f)
import re
hook_count = 0
for event, groups in h.items():
    for group in groups:
        for hook in group.get('hooks', []):
            cmd = hook.get('command', '')
            assert '${CLAUDE_PLUGIN_ROOT}' in cmd, f'hook in {event} missing CLAUDE_PLUGIN_ROOT: {cmd}'
            hook_count += 1
assert hook_count == 8, f'expected 8 hooks, found {hook_count}'

print('All validations passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - .claude-plugin/marketplace.json contains "name": "hlwqds"
    - .claude-plugin/marketplace.json contains "source": "./"
    - .claude-plugin/marketplace.json plugins array has exactly 1 entry
    - .claude-plugin/plugin.json contains "license": "MIT"
    - .claude-plugin/plugin.json contains "homepage": "https://github.com/hlwqds/notify-research"
    - .claude-plugin/plugin.json contains "repository": "https://github.com/hlwqds/notify-research"
    - .claude-plugin/plugin.json author field is object with "name" key (not a string)
    - .claude-plugin/plugin.json keywords array matches ["notification", "audio", "voice", "chinese", "tts"]
    - .claude-plugin/plugin.json version is still "1.4.0" (not 1.5.0)
    - .claude-plugin/plugin.json userConfig.voice preserved unchanged
    - hooks/hooks.json all 8 commands contain "${CLAUDE_PLUGIN_ROOT}"
  </acceptance_criteria>
  <done>marketplace.json created with name=hlwqds, owner info, and plugin entry (source=./, keywords, category=productivity). plugin.json enriched with author object, license=MIT, homepage, repository, and keywords. Both files are valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Validate plugin and verify hooks regression-free</name>
  <files>.claude-plugin/marketplace.json, .claude-plugin/plugin.json, hooks/hooks.json</files>
  <read_first>
    - .claude-plugin/marketplace.json (just created, verify it validates)
    - .claude-plugin/plugin.json (just enriched, verify it validates)
    - hooks/hooks.json (verify no regression from v1.4)
  </read_first>
  <action>
Run `claude plugin validate .` from the repo root directory to validate all plugin artifacts. This command checks plugin.json schema, hooks.json schema, and marketplace.json schema. It must exit with zero errors.

If validation produces errors:
1. Read the error message carefully
2. Cross-reference with RESEARCH.md pitfalls (Pitfall 1: relative path confusion, Pitfall 2: author field type, Pitfall 3: file placement, Pitfall 4: strict mode conflict, Pitfall 5: wrong directory)
3. Fix the identified file and re-run validation
4. Common fixes:
   - "Path contains '..'" -> change source from "../" to "./"
   - Schema error on author -> ensure it's {"name": "..."} not a bare string
   - "File not found" -> ensure marketplace.json is at .claude-plugin/marketplace.json (not repo root)

Also verify hooks.json has no regression by confirming all 8 hook commands contain "${CLAUDE_PLUGIN_ROOT}" (the grep command below does this).

NOTE: If `claude plugin validate .` produces warnings (not errors), those are acceptable. Only ERROR-level failures need fixing. The phase success criteria requires "zero errors", not "zero warnings".
  </action>
  <verify>
    <automated>cd /home/huanglin/code/claude-config/notify-research && claude plugin validate . 2>&1; echo "EXIT_CODE=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `claude plugin validate .` exits with code 0 (or reports zero errors)
    - grep -c 'CLAUDE_PLUGIN_ROOT' hooks/hooks.json returns at least 8 matches
    - No ERROR-level output from claude plugin validate (warnings are acceptable)
  </acceptance_criteria>
  <done>`claude plugin validate .` passes with zero errors. All 8 hooks in hooks.json confirmed using ${CLAUDE_PLUGIN_ROOT}. Plugin artifacts are marketplace-ready.</done>
</task>

</tasks>

<verification>
1. `.claude-plugin/marketplace.json` exists and is valid JSON with name=hlwqds
2. `.claude-plugin/plugin.json` has author, license, homepage, repository, keywords fields
3. `hooks/hooks.json` unchanged from v1.4 (all ${CLAUDE_PLUGIN_ROOT} paths intact)
4. `claude plugin validate .` passes with zero errors
</verification>

<success_criteria>
- marketplace.json has `name: "hlwqds"`, owner with name+email, and plugins array with 1 entry
- Plugin entry has `source: "./"`, `keywords`, and `category: "productivity"`
- plugin.json has `author` (object), `license: "MIT"`, `homepage`, `repository`, `keywords`
- plugin.json `userConfig` preserved unchanged from v1.4
- plugin.json version is 1.4.0 (NOT 1.5.0)
- hooks.json all 8 commands use `${CLAUDE_PLUGIN_ROOT}`
- `claude plugin validate .` reports zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-marketplace/16-01-SUMMARY.md`
</output>
