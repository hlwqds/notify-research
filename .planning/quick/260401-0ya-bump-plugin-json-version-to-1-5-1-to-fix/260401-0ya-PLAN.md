---
phase: quick
plan: 0ya
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
autonomous: true
requirements: [VERSION-BUMP]
must_haves:
  truths:
    - "plugin.json version field reads 1.5.1"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin metadata with bumped version"
      contains: '"version": "1.5.1"'
  key_links: []
---

<objective>
Bump plugin.json version from 1.5.0 to 1.5.1 to invalidate stale hooks.json cache.

Purpose: Claude Code caches hooks.json based on plugin version. After the fix that wrapped hooks.json with a top-level "hooks" key (commit e6636bc), installed plugins still use the old cached version. Bumping the version forces Claude Code to re-read the corrected hooks.json.

Output: Updated plugin.json with version 1.5.1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude-plugin/plugin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump plugin.json version to 1.5.1</name>
  <files>.claude-plugin/plugin.json</files>
  <action>
    In `.claude-plugin/plugin.json`, change line 3 from `"version": "1.5.0"` to `"version": "1.5.1"`. Do not modify any other fields.
  </action>
  <verify>
    <automated>grep -q '"version": "1.5.1"' .claude-plugin/plugin.json</automated>
  </verify>
  <done>plugin.json version field is 1.5.1, all other fields unchanged</done>
</task>

</tasks>

<verification>
```bash
grep '"version"' .claude-plugin/plugin.json
```
Expected output: `"version": "1.5.1"`
</verification>

<success_criteria>
plugin.json version bumped to 1.5.1, triggering Claude Code to refresh cached hooks.json on next load.
</success_criteria>

<output>
After completion, create `.planning/quick/260401-0ya-bump-plugin-json-version-to-1-5-1-to-fix/260401-0ya-SUMMARY.md`
</output>
